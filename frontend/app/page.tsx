"use client";

export default function Home() {
  // Routing logic is now centralized in components/RoleGuard.tsx
  // This component will not render as RoleGuard handles redirects from '/'
  return null;
}
